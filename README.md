# Description

Simple timer for linux, hopefully with notifications integration in the future.
Consists of timer binary and master timer binary.

# Dependencies

- git
- cmake 3.22 or higher
- gcc or different c compiler

<!-- TODO: write down other deps -->

# Build

```bash
cmake -B build && cmake --build build -j9
```
